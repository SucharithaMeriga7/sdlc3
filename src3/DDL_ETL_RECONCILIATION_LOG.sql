/*=========================================================
Object Name : DDL_ETL_RECONCILIATION_LOG
Purpose     : ETL reconciliation log DDL
Author      : SDLC_AGENT
Generated By: SDLC_AGENT
Generated On: 2026-09-11
=========================================================*/

-- ============================================================
-- FILE        : DDL_ETL_RECONCILIATION_LOG.sql
-- DATABASE    : GEN_AI_POC_SNOWFLAKECOE
-- SCHEMA      : SDLC_WIZARD
-- WAREHOUSE   : SNOWFLAKE_LEARNING_WH
-- OBJECT      : ETL_RECONCILIATION_LOG (PIPELINE AUDIT TABLE)
-- PURPOSE     : Captures execution metrics per pipeline run
-- ============================================================

USE DATABASE GEN_AI_POC_SNOWFLAKECOE;
USE SCHEMA SDLC_WIZARD;
USE WAREHOUSE SNOWFLAKE_LEARNING_WH;

-- SECTION 1: AUDIT LOG TABLE
CREATE TABLE IF NOT EXISTS GEN_AI_POC_SNOWFLAKECOE.SDLC_WIZARD.ETL_RECONCILIATION_LOG (
    LOG_ID                  NUMBER AUTOINCREMENT PRIMARY KEY,
    PIPELINE_NAME           VARCHAR(200),
    RUN_START_TIMESTAMP     TIMESTAMP_NTZ,
    RUN_END_TIMESTAMP       TIMESTAMP_NTZ,
    WATERMARK_FROM          TIMESTAMP_NTZ,
    WATERMARK_TO            TIMESTAMP_NTZ,
    SOURCE_ROW_COUNT        NUMBER(12),
    DEDUPLICATED_ROW_COUNT  NUMBER(12),
    EXCLUDED_ROW_COUNT      NUMBER(12),
    INSERTED_ROW_COUNT      NUMBER(12),
    UPDATED_ROW_COUNT       NUMBER(12),
    SOFT_DELETED_ROW_COUNT  NUMBER(12),
    TARGET_ROW_COUNT        NUMBER(12),
    STATUS                  VARCHAR(20),
    ERROR_MESSAGE           VARCHAR(4000),
    EXECUTED_BY             VARCHAR(200)   DEFAULT CURRENT_USER()
);

-- SECTION 2: CLUSTERING
ALTER TABLE GEN_AI_POC_SNOWFLAKECOE.SDLC_WIZARD.ETL_RECONCILIATION_LOG
    CLUSTER BY (RUN_START_TIMESTAMP);

-- SECTION 3: GRANTS
GRANT SELECT, INSERT ON TABLE GEN_AI_POC_SNOWFLAKECOE.SDLC_WIZARD.ETL_RECONCILIATION_LOG
    TO ROLE SYSADMIN;