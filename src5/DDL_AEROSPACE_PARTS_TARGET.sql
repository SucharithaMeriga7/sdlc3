/*=========================================================
Object Name : DDL_AEROSPACE_PARTS_TARGET
Purpose     : Target table DDL
Author      : SDLC_AGENT
Generated By: SDLC_AGENT
=========================================================*/

USE WAREHOUSE SNOWFLAKE_LEARNING_WH;
USE DATABASE GEN_AI_POC_SNOWFLAKECOE;
USE SCHEMA SDLC_WIZARD;

-- SECTION 1: Object Definition
CREATE TABLE IF NOT EXISTS AEROSPACE_PARTS_TARGET (
    PART_NUMBER          VARCHAR(50)     NOT NULL,
    MANUFACTURER         VARCHAR(100),
    WEIGHT_KG            NUMBER(10,4),
    UNIT_PRICE_USD       NUMBER(14,2),
    LIFECYCLE_STATUS     VARCHAR(50),
    INSTALLATION_DATE    DATE,
    CERTIFICATION_STATUS VARCHAR(50),
    LEAD_TIME_DAYS       NUMBER(6,0),
    RISK_SCORE           VARCHAR(20),
    RECORD_STATUS        VARCHAR(30)     DEFAULT 'Active',
    UPDATED_AT           TIMESTAMP_NTZ,
    DW_INSERT_TIMESTAMP  TIMESTAMP_NTZ   DEFAULT CURRENT_TIMESTAMP(),
    DW_UPDATE_TIMESTAMP  TIMESTAMP_NTZ   DEFAULT CURRENT_TIMESTAMP()
);

-- SECTION 2: Constraints
ALTER TABLE AEROSPACE_PARTS_TARGET
    ADD CONSTRAINT IF NOT EXISTS PK_APT_TARGET PRIMARY KEY (PART_NUMBER);

-- SECTION 3: Notes
-- SCD Type 1   : Overwrite on PART_NUMBER match
-- RISK_SCORE   : Derived from CERTIFICATION_STATUS and LEAD_TIME_DAYS
-- RECORD_STATUS: 'Active' | 'Decommissioned' (soft delete for missing source records)
-- DW timestamps: DW_INSERT_TIMESTAMP set on first insert; DW_UPDATE_TIMESTAMP refreshed on every merge